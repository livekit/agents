# Cohere plugin for LiveKit Agents

Support for Cohere's Command models for chat completion and embeddings.

## Installation

```bash
pip install livekit-plugins-cohere
```

## Pre-requisites

You'll need an API key from Cohere. It can be set as an environment variable: `COHERE_API_KEY`
