name: Publish livekit-plugins-anthropic

on:
  workflow_dispatch

jobs:
  call-publish-workflow:
    uses: ./.github/workflows/publish-package.yml
    with:
      package_name: 'livekit-plugins/livekit-plugins-anthropic'
      package_module: 'livekit.plugins.anthropic'
    secrets: inherit
