"""Opt-in debugging tooling for agent workers.

Submodules are imported explicitly (e.g. ``from livekit.agents.debug import memory``)
so that ``python -m livekit.agents.debug.memory`` runs without re-import warnings.
"""
