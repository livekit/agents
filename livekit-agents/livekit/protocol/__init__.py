"""Minimal shim package livekit.protocol to satisfy imports during tests.
This provides very small placeholder modules; expand them later if runtime code needs behavior."""
__all__ = ["agent", "models"]
