"""Shim: livekit.protocol.models
Provide minimal placeholder dataclasses/objects referenced by the code."""
class ModelPlaceholder:
    def __init__(self, *args, **kwargs):
        self._kwargs = kwargs

# Add more placeholders later if required by tests.
